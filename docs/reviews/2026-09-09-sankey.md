# Sankey implementation review

The `sankey` catalog entry accepts annotated nodes and weighted links and renders through the
trusted HTML adapter. Pydantic props, generated JSON Schema, prompt documentation, renderer
dispatch, shipped assets and analytics documentation share the same contract. The sales example
uses registered provider fields, with an explicit scope independent of its regional row filter.

Implemented controls include bounded acyclic topology, unique identifiers and endpoint pairs,
declared endpoints, non-negative finite numeric quantities, runtime expression-result checks,
escaped SVG/text, component-local data errors, and registered navigation drilldowns. Geometry
uses a common quantity scale and reports imbalances without manufacturing balancing flows.

## Final presentation

Ribbons blend between six desaturated theme colors using renderer-owned, instance-unique SVG
paint IDs. Gradient stops and ribbon opacity give approximately 10–21% effective opacity at rest.
Labels remain above the 18 px rounded node bars. The arrow cursor and mouse-selection prevention
keep inspection unobtrusive; keyboard focus and tooltip inspection remain available.

Flow data uses a compact muted disclosure, connection/node counts and a small chevron. The final
pass removes the filled hover treatment and table-header backgrounds. Captions have 16 px padding
aligned with the cells, with separate metadata. Numeric columns align right, row labels use normal
case, and single light rules separate rows. Keyboard focus remains visible.

## Final verification

The staged source was exported to an isolated checkout, excluding pre-existing compact-catalog
work. Commands below ran there with `PYTHONPATH=src` and the repository's existing `.venv/bin`
executables. Browser and non-browser suites ran in separate processes, matching CI.

| Command | Result |
| --- | --- |
| `python -m pytest -q -m 'not browser and not docker and not ollama' --cov=forgeui --cov-report=term-missing:skip-covered` | 534 passed; 19 deselected; 89.76% coverage |
| `python -m pytest -q -m browser --basetemp=/tmp/forgeui-sankey-push-browser` | 18 passed; 535 deselected |
| `ruff format --check .` | Passed: 144 files |
| `ruff check .` | Passed |
| `mypy src/forgeui` | Passed: 59 source files |
| `bandit -q -r src/forgeui` | Passed |
| `git diff --cached --check` | Passed |

Browser coverage includes inert/events fragments, desktop/mobile at 1280/390 pixels, light/dark
themes, keyboard node/ribbon inspection, Escape dismissal, native disclosure, caption alignment,
mouse-selection prevention, bounded horizontal scrolling, reduced motion and 3:1 node-bar contrast.
Screenshots were visually inspected, including the final light-theme Flow data presentation.

The scoped chart stylesheet has explicit 4 KiB raw/1.5 KiB gzip budgets. Core asset limits are
unchanged. CI and publishing wheel limits increase from 144 to 152 KiB for Sankey support. No
runtime dependency was added. This source push leaves the package version unchanged and does
not build or publish a new package.

## Failures encountered and disposition

- Initial provider-value fixtures used a nonexistent data path (8 failures); they now use the
  existing `data.summary.total` contract path.
- Existing donut browser tests selected the first tooltip after insertion of the Sankey
  (2 failures); selectors now target their chart.
- An invalid drilldown fixture used `set_state`; it now uses registered `navigate`, preserving
  the existing action policy.
- A contrast check treated a transparent background as black; it now resolves the opaque ancestor.
- Click checks left focus tests in pointer modality (2 failures); tests now press Tab before
  keyboard inspection.
- The expanded stylesheet exceeded its former 1 KiB gzip budget. Explicit scoped budgets were
  updated as noted above; the footprint check passes.
- A combined browser/non-browser command, `uv run pytest -q -m 'not docker and not ollama'
  --cov=forgeui --cov-report=term-missing:skip-covered
  --basetemp=/tmp/forgeui-sankey-verification`, returned 15 failures/554 passes. Sync Playwright
  and pytest-asyncio conflicted: `Runner.run() cannot be called from a running event loop`.
  Both suites pass with the existing CI process split. Combined execution remains a test-runner
  limitation; unrelated runtime code was not changed to address it.
- Passing runs emit the existing Starlette/httpx TestClient deprecation warning. Live Ollama and
  Docker checks were not run.

## Scope and limitations

Sankey supports at most 40 nodes and 80 links, one common unit, and an acyclic left-to-right layout.
It does not optimize crossings or apply local row filters. Zero and very small flows retain table
annotations; subpixel ribbons can be difficult to see. Hosts aggregate upstream and publish new
immutable revisions when topology changes. Portable hosts load `forgeui-charts.css` from the
adapter's asset list.

The user reviewed the local styling and authorized a source push after the final subtle Flow data
pass. Pre-existing compact catalog, prompt and architecture edits remain local and are excluded.

## Container-width follow-up

The SVG now expands proportionally to fill wider containers. Final-column labels face inward,
replacing the oversized trailing margin with equal 30-unit insets. Narrow containers retain
horizontal scrolling for legibility. Compact diagrams grow vertically with their aspect ratio;
dense graphs retain the bounded 340 px scroll viewport.

Verification (all passed, no test failures):

- `.venv/bin/pytest -q tests/unit/renderer/test_sankey.py tests/contracts/test_footprint.py`:
  39 passed.
- `.venv/bin/pytest -q -m browser tests/integration/test_sankey_browser.py
  tests/integration/test_analytics_browser.py --basetemp=/tmp/forgeui-sankey-width`:
  9 passed. Regression checks verify full desktop width, equal outer insets, no vertical clipping
  of the sample, and mobile containment alongside existing interaction/theme coverage.
- `.venv/bin/ruff format src/forgeui/renderer/sankey.py tests/integration/test_sankey_browser.py`:
  both files unchanged.
- `.venv/bin/ruff check src/forgeui/renderer/sankey.py tests/integration/test_sankey_browser.py`,
  `.venv/bin/mypy src/forgeui/renderer/sankey.py`, and `git diff --check`: passed.

The local preview server returned empty responses and was restarted. The refreshed root preview
was visually inspected. No package build or release was performed.

## Stable typography and brighter dark mode

Node labels and values now render in a separate, unscaled SVG annotation layer at 13 px and
12 px. Percentage anchors track the expanding flow geometry, while fixed baseline offsets keep
text above its bars. This works in inert fragments without JavaScript. The geometry retains its
accessible node names and keyboard targets; the visual annotation layer avoids duplicate screen
reader content and keeps hover inspection and the regular cursor.

Dark colors mix toward the light ink token, and dark ribbons use 30–50% gradient opacity.
Explicit dark mode and system dark mode share that treatment. Light colors and transparency
remain unchanged. The chart stylesheet's raw budget rises from 4 to 5 KiB for the annotation and
theme rules; its 1.5 KiB compressed budget still passes. No dependency or package version changed.

Verification:

- `.venv/bin/pytest -q tests/unit/renderer/test_sankey.py tests/contracts/test_footprint.py
  tests/security/test_renderer_security.py`: 41 passed.
- `.venv/bin/pytest -q -m browser tests/integration/test_sankey_browser.py
  tests/integration/test_analytics_browser.py --basetemp=/tmp/forgeui-sankey-type-dark-verified`:
  10 passed. Includes explicit/system dark themes, 390/1280 px viewports, live resizing to
  1600 px, fixed rendered text size, at least 4.5:1 annotation contrast and 3:1 bar contrast,
  label hover, keyboard inspection and mouse selection prevention.
- `.venv/bin/ruff check src/forgeui/renderer/sankey.py tests/unit/renderer/test_sankey.py
  tests/integration/test_sankey_browser.py tests/contracts/test_footprint.py`: passed.
- `.venv/bin/ruff format --check src/forgeui/renderer/sankey.py
  tests/integration/test_sankey_browser.py tests/unit/renderer/test_sankey.py`: passed.
- `.venv/bin/mypy src/forgeui/renderer/sankey.py` and `git diff --check`: passed.

An intermediate browser run had three hover-test failures because the pointer was already on the
label before the event script was attached. Moving it away before the hover check makes the test
exercise an actual pointer entry. A formatting check flagged the new cursor assertion; formatting
was applied and rechecked. Light/dark screenshots and the live dark preview were visually inspected.
